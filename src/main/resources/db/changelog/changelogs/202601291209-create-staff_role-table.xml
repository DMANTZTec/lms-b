<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.15.xsd">

    <changeSet id="202601291209" author="shiva">

        <!-- Create staff_role table -->
        <createTable tableName="staff_role">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="staff_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="role_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="created_by" type="BIGINT"/>
            <column name="created_dt" type="TIMESTAMP"/>

            <column name="updated_by" type="BIGINT"/>
            <column name="updated_dt" type="TIMESTAMP"/>
        </createTable>

        <!-- Indexes (REQUIRED for MySQL FK) -->
        <createIndex tableName="staff_role" indexName="idx_staff_role_staff">
            <column name="staff_id"/>
        </createIndex>

        <createIndex tableName="staff_role" indexName="idx_staff_role_role">
            <column name="role_id"/>
        </createIndex>

        <!-- Foreign Keys -->
        <addForeignKeyConstraint
                constraintName="fk_staff_role_staff"
                baseTableName="staff_role"
                baseColumnNames="staff_id"
                referencedTableName="staff"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                constraintName="fk_staff_role_role"
                baseTableName="staff_role"
                baseColumnNames="role_id"
                referencedTableName="role"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Prevent duplicate role assignment -->
        <addUniqueConstraint
                tableName="staff_role"
                columnNames="staff_id, role_id"
                constraintName="uk_staff_role_unique"/>

    </changeSet>

</databaseChangeLog>
